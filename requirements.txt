# Core — needed for `run --mock --scorer keyword` (no API key required).
requests>=2.31
PyYAML>=6.0

# Only needed if you point a stage at Anthropic (the default provider).
# Gemini, Groq, DeepSeek, OpenAI-compatible and Ollama all go over plain
# `requests`, so you can delete this line and still use those.
anthropic>=0.40

# Web service for Render (the /run endpoint a free cron pings 4x/day).
# Not needed for local CLI runs or GitHub Actions.
flask>=3.0
gunicorn>=21.2

# Tests
pytest>=8.0
